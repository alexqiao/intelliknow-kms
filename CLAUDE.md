Please remember the following strict Git workflow and formatting rules for all future code submissions in this project:

1. **NO DIRECT COMMITS TO MAIN**: Never commit directly to the `main` or `master` branch. 
2. **USE PR WORKFLOW**: When I ask you to submit, commit, or push code, you must:
   - Create and checkout a new branch with a descriptive name (e.g., `git checkout -b fix/slack-troubleshooting-docs`).
   - Stage the changes (`git add`).
   - Create a commit.
   - Push the branch to the remote repository.
   - Tell me the branch is pushed so I can open a PR, OR use the `gh` CLI (if available) to create a Pull Request automatically.
3. **NO AI MENTIONS**: Do NOT mention "Claude", your model name, or any AI tool in the commit message. 
4. **NO CO-AUTHORS**: Do NOT append "Co-authored-by" or modify the Git Author identity. Use the default local git user configuration. The commit message should be strictly professional, concise, and focused purely on the technical changes.